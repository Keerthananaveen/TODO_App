{
    "version": 2,
    "builds": [{
        "src": "src/server.ts",
        "use": "@vercel/node",
        "outputDirectory": "public",

        "config": { "includeFiles": ["dist/**"] }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "src/server.ts"
    }]
}